// compare Num_one and Num_two and return the smaller of the two
function min( Num_one, Num_two ) {
	if ( Num_one < Num_two )
      return Num_one;
    else
      return Num_two;
}

console.log(min(25, 8));
// print the minimum
console.log(min(72, 12));
// print the largest